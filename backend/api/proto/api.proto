syntax = "proto3";
package gemify;

option go_package = "gemify/api";

message Message {
  string convoId = 1;
  string content = 2;
  bool is_user = 3;
}

service GemifyAPI {
  // Sends a new msg and gets the reply
  rpc SendMessage(Message) returns (stream Message) {}
}
